interface:
  display_name: "货柜成本计算"
  short_description: "对整理完成的标准货柜表配置费用、计算成本并按授权导入系统"
  default_prompt: "Use $container-cost to validate this prepared container workbook, apply the documented cost parameters, and calculate its K2046 import costs."
